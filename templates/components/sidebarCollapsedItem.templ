package components

templ CollapsedSidebarItem(icon string) {
    <a 
        href="#"
        class="p-2 rounded-lg hover:bg-gray-300 dark:hover:bg-gray-600 text-gray-700 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400 transition-colors"
        title={ icon }>
        <span class="material-icons">{ icon }</span>
    </a>
}
